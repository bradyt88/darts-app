import React from "react";
import { createRoot } from "react-dom/client";
import DarkMind from "../DarkMind.jsx";

createRoot(document.getElementById("root")).render(
  <React.StrictMode><DarkMind /></React.StrictMode>
);
